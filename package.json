{
  "name": "@gigradar/design-system",
  "version": "0.0.0",
  "private": true,
  "description": "GigRadar design system \u2014 tokens, components, and rules for home, dashboard, crm, and inbound",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @gigradar/gallery",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc --build --force",
    "lint": "ESLINT_USE_FLAT_CONFIG=true eslint .",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "npm run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.9",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^8.57.1",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18"
  }
}
